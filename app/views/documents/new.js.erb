$('#content').replaceWith( '<%= j render("shared/update_data", data_object: @document) %>');

pageSetUp();

<% if flash.any? %>
  $('#content').before('<%= j render("shared/messages") %>');
<% end %>

setTimeout(function() {
  $('.flash-messages').remove();
}, 10000);