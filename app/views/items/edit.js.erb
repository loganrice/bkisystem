$('#content').replaceWith( '<%= j render("form") %>');
<% unless flash.empty? %>
  $('#content').before('<%= j render("shared/messages") %>');

  setTimeout(function() {
    $('.flash-messages').remove();
  }, 10000);
<% end %>

