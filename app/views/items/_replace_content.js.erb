  $('#content').replaceWith( '<%= j render(#{view}) %>');
  $('#content').before('<%= j render("shared/messages") %>');
  setTimeout(function() {
    $('#flash-messages').remove();
  }, 10000);
  pageSetUp();
