$('#content').replaceWith( '<%= j render "form", certificate_of_origin: @certificate_of_origin %>');
pageSetUp();

<% if flash.any? %>
  $('#content').before('<%= j render("shared/messages") %>');
<% end %>

setTimeout(function() {
  $('.flash-messages').remove();
}, 10000);