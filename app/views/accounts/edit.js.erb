$('#content').replaceWith( '<%= j render("form") %>');
pageSetUp();

<% if flash.any? %>
  $('#content').before('<%= j render("shared/messages") %>');
<% end %>

setTimeout(function() {
  $('.flash-messages').remove();
}, 10000);

$('#country-select').on('change', function(event) {
  var country_code = $('#country-select').val().toLowerCase();
  $('#flag').removeClass();
  $('#flag').addClass("flag flag-" + country_code);
  alert(x);
});